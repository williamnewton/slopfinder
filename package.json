{
  "name": "slopfinder",
  "version": "0.2.0",
  "private": true,
  "description": "LLM Cliché Highlighter — Chrome extension + landing site. Highlights the tells of AI-generated prose.",
  "type": "module",
  "scripts": {
    "check": "node scripts/check.mjs",
    "test": "node --test tests/*.test.mjs",
    "package": "node scripts/package.mjs",
    "build": "npm run check && npm run test && npm run package",
    "serve": "node scripts/serve.mjs",
    "bump": "node scripts/bump-version.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
